<Window x:Class="PlGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlGui"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="500">
    <Window.Resources>
        <Style x:Key="myButtonStyle"
           TargetType="TextBox">
            <Setter Property="Foreground"
              Value="Green" />
            <Setter Property="Background" Value="#FF1CFCFC"/>
            <Setter Property="VerticalAlignment"
              Value="Center" />
            <Setter Property="HorizontalAlignment"
              Value="Stretch" />
            <Setter Property="FontSize" Value="19"/>
            <Setter Property="FontFamily" Value="Cooper Black"/>
        </Style>
    </Window.Resources>
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" MappingMode="RelativeToBoundingBox" StartPoint="0.5,0">
            <GradientStop Color="White" Offset="1"/>
            <GradientStop Color="#FF0BFA1E"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>

        </Grid.RowDefinitions>
        <TextBox x:Name="bn_management" Text="enter your username" Grid.Column="1" Grid.Row="1" Style="{StaticResource myButtonStyle}" />
        <TextBox x:Name="bn_user" Text="enter your password" Grid.Column="1" Grid.Row="2" Style="{StaticResource myButtonStyle}"/>
        <Label x:Name="lbl__log" Content="Log in" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Bottom" FontSize="50" FontStyle="Italic" FontFamily="Brush Script MT"/>

    </Grid>
</Window>
